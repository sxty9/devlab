// AtlasPorts — Welle-0 stub (B5 fills it). Observed port allocations with conflicts named —
// derived fresh from routes + /proc/net/tcp, never stored (F9).
export function AtlasPorts() {
  return (
    <div className="flex h-full min-h-0 items-center justify-center p-8">
      <p className="max-w-md text-center text-footnote text-text-tertiary">
        AtlasPorts arrives with its building block (B5).
      </p>
    </div>
  );
}
