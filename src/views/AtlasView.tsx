import { CapabilityPlaceholder } from '@/views/CapabilityPlaceholder';
import { AtlasIcon } from '@/ui/icons';

/** Atlas — the model of how Holistic services interact. */
export function AtlasView() {
  return <CapabilityPlaceholder icon={AtlasIcon} title="Atlas" note="Service interactions" />;
}
