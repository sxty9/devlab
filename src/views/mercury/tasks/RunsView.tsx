// RunsView — Welle-0 stub (B8 fills it). The automatic-runs surface over TaskForm/TaskList (one machinery, REQ-005).
export function RunsView() {
  return (
    <div className="flex h-full min-h-0 items-center justify-center p-8">
      <p className="max-w-md text-center text-footnote text-text-tertiary">
        RunsView arrives with its building block (B8).
      </p>
    </div>
  );
}
