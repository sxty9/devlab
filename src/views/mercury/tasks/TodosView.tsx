// TodosView — Welle-0 stub (B8 fills it). The todos surface over TaskForm/TaskList (one machinery, REQ-005).
export function TodosView() {
  return (
    <div className="flex h-full min-h-0 items-center justify-center p-8">
      <p className="max-w-md text-center text-footnote text-text-tertiary">
        TodosView arrives with its building block (B8).
      </p>
    </div>
  );
}
