// TaskForm — Welle-0 stub (B8 fills it). The SHARED create/edit form of both kinds — targets incl. new repos, attachments (file dialog + clipboard as equals), due "now", the three tunables via ui/RunTuning (S6).
export function TaskForm() {
  return (
    <div className="flex h-full min-h-0 items-center justify-center p-8">
      <p className="max-w-md text-center text-footnote text-text-tertiary">
        TaskForm arrives with its building block (B8).
      </p>
    </div>
  );
}
