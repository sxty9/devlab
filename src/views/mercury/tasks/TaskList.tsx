// TaskList — Welle-0 stub (B8 fills it). The SHARED list building block of both kinds (filters, authorship, open ∩ history = ∅).
export function TaskList() {
  return (
    <div className="flex h-full min-h-0 items-center justify-center p-8">
      <p className="max-w-md text-center text-footnote text-text-tertiary">
        TaskList arrives with its building block (B8).
      </p>
    </div>
  );
}
