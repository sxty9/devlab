// StartDialog — Welle-0 stub (B9 fills it). Start into contended slots: queue / defer (with reasoned suggestion) / overload (REQ-015).
export function StartDialog() {
  return (
    <div className="flex h-full min-h-0 items-center justify-center p-8">
      <p className="max-w-md text-center text-footnote text-text-tertiary">
        StartDialog arrives with its building block (B9).
      </p>
    </div>
  );
}
