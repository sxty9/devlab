// ActiveList — Welle-0 stub (B9 fills it). ACTIVE executions as a LIST with live transcript — SSE-driven, never polled (REQ-034).
export function ActiveList() {
  return (
    <div className="flex h-full min-h-0 items-center justify-center p-8">
      <p className="max-w-md text-center text-footnote text-text-tertiary">
        ActiveList arrives with its building block (B9).
      </p>
    </div>
  );
}
