import { CapabilityPlaceholder } from '@/views/CapabilityPlaceholder';
import { MercuryIcon } from '@/ui/icons';

/** Mercury — the centre for the Holistic axioms. */
export function MercuryView() {
  return <CapabilityPlaceholder icon={MercuryIcon} title="Mercury" note="Holistic axioms" />;
}
